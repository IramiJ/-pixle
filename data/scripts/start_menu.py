import pygame
pygame.init()

class start_menu():
    def __init__(self):
        self.screen_size = (200, 120)
        self.screen = pygame.display.set_mode(self.screen_size)
        self.input_rect = pygame.Rect(50, 30, 100, 60)

    def render():
        self.screen.draw.rect()
